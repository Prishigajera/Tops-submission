Que 9 ) Find circumference of Triangle formula : triangle = a + b + c

Ans)

#include<stdio.h>
int main(){
    float length,width,parameter;
    printf("enter the length of the rectangle: ");
    scanf("%f,&length");
    printf("enter the width of the rectangle: ");
    scanf("%f",&width);
    perameter = 2*
    peintf("the parameter of the rectangle is:%.2f\n",perameter);
    return 0;
}